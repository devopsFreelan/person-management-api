The CI workflow runs `mvn test` on push and pull requests to `main`. The badge in README shows the workflow status.

If you want to enable the badge, push this repo to GitHub under the `devopsFreelan/person-management-api` repo name and the badge will show results from GitHub Actions.
